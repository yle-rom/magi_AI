MAGI_DB_PASS=changeme

# Where MAGI-CODE can read/list files by default (comma-separated for
# multiple roots). Defaults to ~/.local/src if unset — override this if your
# projects live somewhere else, e.g.:
# MAGI_CODE_ROOT=/home/you/projects,/home/you/work
